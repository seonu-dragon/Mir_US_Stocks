# MIR AI 모드 — 비전 & 구현 로드맵

> 참고 이미지: `data/참고/1.png`  
> 기능 목록: `FEATURES.md`

---

## 1. 왜 AI 모드를 만드는가

Mir_US_Stocks 웹사이트에는 시장 히트맵, 종목 차트, 스크리너, 포트폴리오, 섹터 분석, 공매도, 기관 보유, 뉴스, AI 브리핑 등 **수십 가지 기능**이 탭과 패널에 흩어져 있다.

실제 사용 시 문제:

- **"엔비디아만 보고 싶다"** → NVDA 관련 정보가 차트·재무·뉴스·시그널·섹터 등 여러 곳에 분산
- 기능을 알아야 찾을 수 있고, 탭을 오가며 조합해야 함
- 정보는 풍부하지만 **탐색 비용**이 높음

**AI 모드의 목적:** 웹사이트에 이미 있는 데이터를 채팅 한 줄로 요청하면, AI가 맥락을 이해하고 **한 화면에 시각화**해 보여준다. 투자 코멘트는 AI가 덧붙인다.

---

## 2. 핵심 컨셉 — 자비스(JARVIS)형 시각화

아이언맨의 자비스처럼:

1. **첫 화면** — 움직이는 3D Loss Landscape가 배경 전체를 차지 (현재 `ai-cosmos.js`)
2. **질문 입력** — "엔비디아 분석해줘" 등 자연어·티커 입력
3. **같은 화면이 변형** — 새 팝업·새 페이지·새 박스를 띄우지 않고, **현재 AI 첫 페이지가 변형**되어 답이 나타남
4. **배경 그래프가 차트로** — 3D 지형이 2D 종목 차트로 **모핑(morph)** 애니메이션
5. **주변 UI 등장** — 차트 주변에 종목 카드, AI 의견, 재무·뉴스 카드가 페이드인 (최종 목표: `data/참고/1.png`)

> **가장 중요한 요소 = 시각화.** 텍스트 답변은 보조이고, 사용자가 "보는" 경험이 중심이다.

---

## 3. 최종 완성 목표 (참고 이미지 기준)

`data/참고/1.png` 레이아웃:

| 영역 | 내용 |
|------|------|
| 상단 | MIR AI 헤더 + 채팅 입력 ("엔비디아 분석해줘") |
| 좌측 | 종목 카드 — 로고, 가격, 등락, 시총·PER·52주 등 |
| 중앙 | **가격 차트** — 캔들, MA5/20/60, 거래량, RSI·MACD 서브차트 |
| 우측 | AI 투자의견 — 매수/매도/중립, 신뢰도, 포인트·리스크 |
| 하단 | 재무 요약, 성장 추이, 밸류에이션, 뉴스, 공시, 목표주가, 피어 비교 |

모든 데이터는 **이미 사이트에 있는** `data/details/`, `market_snapshot`, `analysis.js` 등에서 가져온다.

---

## 4. 구현 원칙

1. **In-place transformation** — 레이아웃 슬롯을 바꾸지 않고 `#aiCosmos` 배경 + 오버레이가 변형
2. **데이터 재사용** — 새 API 없이 기존 JSON·JS 모듈 활용
3. **단계적 구현** — 애니메이션 → 차트 → 패널 → AI 코멘트 순
4. **시각 우선** — CSS `ai-stock-dashboard` (`asd-*`) 클래스는 이미 준비됨

---

## 5. 구현 단계 (로드맵)

### Phase 1 — 3D → 2D 차트 모핑 ✅ (완료)

- [x] 비전 문서 정리
- [x] 사용자가 종목 입력 시 티커 해석 (`엔비디아` → `NVDA`)
- [x] `data/details/{TICKER}.json` 에서 `chartSeries` 로드
- [x] `ai-cosmos.js`: Loss Landscape → 2D 캔들차트 모핑 애니메이션 (~1.8초)
- [x] `ai-mode-welcome.js`: 폼 제출 → 모핑 트리거
- [x] AI 모드 종료 시 배경 리셋

**기술 요약:**

```
[3D Landscape]  ──camera flatten + surface morph──▶  [2D Candlestick Chart]
     epoch loop                    morphT 0→1              same #aiCosmos canvas
```

### Phase 2 — 대시보드 오버레이 ✅ (완료, 2026-07 실사)

- [x] 인라인 종목 위젯 + 데이터 패널 25종+ (`renderAiModeDataBoard`, app.js 18458~19111
      — 기술지표·밸류·뉴스·공시·공매도·컨센서스·옵션·DCF·체력 레이더·피어 비교 등)
- [x] 차트 위젯(캔들·MA·거래량) + 크로스헤어

### Phase 3 — AI 투자 코멘트 ✅ (완료)

- [x] `LIVE_DATA_PROXY`(워커 /chat, Gemini 폴백 포함) 연동
- [x] 종목 컨텍스트 전달(stockContext) — 단, 매수/매도 추천은 정책상 하지 않음

### Phase 4 — 인터랙션 확장 (일부 진행)

- [x] 후속 질문 칩
- [x] 차트 기간 탭 (1M / 3M / 6M / 1Y / 5Y — 2026-07-24)
- [ ] 한국 주식(KR) 모드 심화 지원

### Phase 5 — 완성 (2026-07-24)

- [x] 참고 레이아웃(1.png) 정보 구조: 상단 입력 · 좌 종목카드 · 중앙 차트(RSI/MACD 서브차트) · 우 AI 의견 · 하단 데이터 그리드
- [x] AI 의견 카드: LLM 요약 + 관찰 포인트/리스크 불릿 (정책상 매수/매도/목표가 추천 없음)
- [x] /chat SSE 스트리밍 + 대화 중단(■) + 구 워커 JSON 자동 폴백
- [x] Ctrl+K 커맨드 팔레트 · 대화 .md 내보내기 · AI 모드 대화 뷰(비종목 질문 가시화)

---

## 6. 파일 구조 (AI 모드 관련)

| 파일 | 역할 |
|------|------|
| `index.html` | `#tab-ai-chat`, `#aiCosmos`, 입력 폼 |
| `ai-cosmos.js` | 3D 배경 + 2D 차트 모핑 렌더러 |
| `ai-mode-welcome.js` | AI 모드 진입/퇴장, 입력 처리 |
| `styles.css` | `body.ai-mode-active`, `asd-*` 대시보드 스타일 |
| `app.js` | 티커 해석, `loadStockDetail()`, 차트 엔진 |
| `data/details/*.json` | 종목별 `chartSeries` (OHLCV) |
| `data/ticker_aliases_ko.js` | 한국어 별칭 → 티커 |

---

## 7. 사용자 시나리오 (Phase 1)

```
1. 사용자가 "AI 모드" 버튼 클릭
2. 3D Loss Landscape 배경 + 입력창 표시
3. "엔비디아" 입력 후 전송
4. 티커 NVDA 해석 → detail JSON 로드
5. 배경 3D 지형이 ~1.8초에 걸쳐 2D NVDA 캔들차트로 변형
6. 차트 위에 티커·기간 라벨 표시
7. "종료" 또는 Escape → 배경 리셋, 일반 모드 복귀
```

---

*최종 업데이트: 2026-07-01*