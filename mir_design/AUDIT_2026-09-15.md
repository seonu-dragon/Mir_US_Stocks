# Mir_US_Stocks 전수 감사 보고서 (2026-09-15)

- 대상: `main` `46cfaa055` (라이브 = 레포 동기, `today_content.json` 양쪽 2026-09-14)
- 방식: 프론트 25개 스크립트·CSS·HTML 전 행, 워커 2,864행, 파이썬 빌더·브리핑·automation 전 파일, 워크플로우 22개를 10개 영역으로 나눠 읽고, 라이브 사이트·워커·데이터 파일을 curl/브라우저로 대조. 코드는 수정하지 않음(읽기 전용).
- 검사 도구 결과: `stamp_build_id --check` OK · `sync_critical_css --check` OK · `check_global_name_collisions` 0건(25 스크립트/1,450 전역) · `check_deploy_triggers` OK(18개) · `check_pattern_parity` PASS(1,443 이벤트) · `pytest scripts/tests` 398 passed/1 skipped · `automation/tests` 9 passed · `node worker/test_worker.mjs` 50/50 · **`build_sitemap --check` FAIL(최신 아님)** · `build_bundle --check` 이 PC 에 Node 없음(CI 통과 확인).
- 라이브 브라우저: 미국·한국 모드 홈 콘솔 오류 0, 실패 요청 0, SW 활성, 번들 `0f88d7607b`.

**한 줄 요약: 감시 장치(테스트·게이트·Actions)는 전부 초록인데, 그 뒤에서 (1) 코스피·코스닥 등락률이 두 경로 모두 틀린 값으로 발행되고, (2) 실적 데이터·Gemini 폴백·국내 브리핑 스크레이퍼가 죽어 있으며, (3) 9월 4일 '상승확률 → 모멘텀 점수' 정직화가 화면 절반과 AI 프롬프트에 미반영이고, (4) 미국 빌더 다수가 실패를 exit 0 으로 삼킨다.**

---

## 1. 지금 당장 고쳐야 할 것 (P0)

| # | 영역 | 위치 | 문제 | 고치기 |
|---|---|---|---|---|
| 1 | 워커 | `worker/yahoo-proxy.js:1500-1516` | `fetchPrevDailyClose` 가 null 제거 후 `closes[len-2]` 를 직전 종가로 씀 → 미국 모드 홈 KOSPI −4.97%(참값 −3.26), KOSDAQ −3.60%(참값 −1.69). 같은 응답 `series` 와 자기모순 | 오버라이드 제거(같은 1d/5m 응답의 `meta.chartPreviousClose` 가 정확함). 유지하려면 timestamp 기준 '현재 세션 이전 마지막 non-null'. 워커는 수동 재배포 필요 |
| 2 | KR 빌더 | `scripts/update_korea_data.py:1578-1579,1617-1620` | `kospi[0]`/`kosdaq[0]` 은 지수가 아니라 **시총 1위 개별 종목**(삼성전자·알테오젠) → 한국 모드 홈 코스피 −4.2%(=삼성전자), 코스닥 −3.7%(=알테오젠). `price` 미방출이라 `app.js:1907-1911` 이 KODEX 200 ETF 가격 105,410 을 코스피 가격으로 표시. 같은 사이트 `korea_close.json` 은 6,684.37(−3.26%) 정확 | m.stock `index/KOSPI/basic`·`index/KOSDAQ/basic` 의 `closePrice`·`fluctuationsRatio` 사용, `price` 방출. `app.js:1909` ETF price 폴백 제거 |
| 3 | 분석 페이지 | `analysis.js:2737-2742, 2729`, `chart.js:388` | 헤드라인 "상승 76% / 하락 24%"가 확률로 표시되나 실체는 기술 점수·실측 가중평균. `prob_calibration.json` US 5/20/60일 `significantBuckets = 0`(근거 없음). 면책은 `<details>`(2716) 안에 접힘 | "기술 점수 N/100 + 과거 실측 X%(표본 n)" 병기, '확률' 단어 제거, 캘리브레이션 박스를 접힘 밖으로 |
| 4 | AI 프롬프트 | `chart-indicators.js:640-648` | 모든 /chat·AI 리포트 컨텍스트에 "MirProb 상승확률 N%()" 주입(`scanQuickProb` 는 모멘텀 점수, `scanVerdict` 미정의라 빈 괄호) | "모멘텀 점수(예측 아님) N/100" 으로 바꾸거나 줄 삭제 |
| 5 | 스캐너 | `app.js:4527` + `index.html` | `#scanRank`·`#scannerRankNote`·`#scannerEvidence` 마크업이 index.html 에 없음(추가된 적 없음) → '검증된 팩터 순위'·검증 근거 UI 전체 도달 불가, `factor_validation.js` 45KB 헛다운로드 | 스캐너 툴바에 마크업 3개 추가(리스너 3360·렌더 4754 준비됨) |
| 6 | SEO | `scripts/build_sitemap.py:132` vs `analysis.js:2979` | sitemap 의 국내 딥링크 300개는 `?t=005930&market=kr`, canonical 은 `?t=005930` → 크롤러가 canonical 로 가면 미국 모드로 떠서 빈 화면. 사이트맵 절반이 무효 | `updateAnalysisMeta` 에서 KR 이면 `&market=kr` 을 canonical·og:url 에 추가(양쪽 함께) |
| 7 | 배포 | `deploy-pages.yml` | 아티팩트 **456MB**(문서 330MB). `data/details` 195M + `korea/details` 169M. no-op 게이트는 30회 중 1회만 발동, 하루 ~30회 업로드. Pages 1GB 한도의 45% | 사이즈 가드(700MB 초과 시 실패+텔레그램) + details 프루닝 또는 R2 이전 |
| 8 | 워커 | `worker/yahoo-proxy.js:844,965-971,1163-1183` | 야후 crumb 세션 라이브 전면 실패(`earnings_probe` cookieLen 0) → **실적 데이터 전멸**, 실패 음성 캐시 없어 `?ticker=` 1회에 30회 넘는 서브리퀘스트 낭비(무료 상한 50), 3.5초 | 실패도 TTL 캐시, crumb 없으면 `fetchEarnings` 즉시 null, in-flight 단일화 |
| 9 | 워커 | `worker/yahoo-proxy.js:234` | Gemini 폴백 `gemini-2.0-flash`(2026-06-01 셧다운)·`gemini-1.5-flash`(2025-09 종료) 둘 다 죽음 → `GEMINI_MODEL` 미설정이면 /chat 마다 404×2 후 Workers AI | 살아 있는 모델로 교체(`gemini-2.5-flash` 는 브리핑에서 이미 사용), DEPLOY.md:95-97 갱신 |
| 10 | KR 브리핑 | `scripts/briefings/korea_close/scrapers.py:41-42,114-115`(premarket 동일) | 네이버 PC 스크레이퍼 2종 사망(실측 0건) → 라이브 `korea_close.json` 수급 "데이터 수집 실패" + AI 가 헤드라인으로 창작("외국인 3.2조 순매도") | m.stock `index/KOSPI/trend`(실측 personal/foreign/institutional 값 제공)로 교체, 지수·수급 모두 비면 exit 1 |
| 11 | KR 브리핑 | `korea_premarket/main.py:34,44-46`, `korea_close/main.py:57` | 프롬프트가 미증시·환율 절을 요구하나 raw_data 에 없음 → 창작(나스닥 '약보합' 실제 +0.96%, 환율 '1350원대' 실제 1,346.4 — 같은 파이프라인 `ecos_macro.json` 에 있음) | `ecos_macro.json`·US 스냅샷 indices 주입 + "없는 수치 금지" 명시 |
| 12 | KR 빌더 | `update_korea_data.py:516-538`, `check_data_freshness.py:130-145` | 유니버스 하한 검사 없음. 정직성 게이트는 `prev_in_scope >= 100` 조건이라 50개 붕괴 시 미발동, ratio 검사도 통과 → 09-10 사고 경로 그대로 | `len(metas) < 3000` 이면 exit 1 + freshness 에 `universeCount >= 3000` |
| 13 | US 빌더 | `build_13f_snapshot.py:201-247` | 기관별 예외를 `status:error` 로 삼킴 → 라이브 131개 중 **error 62(47%)** 인데 초록. `write_data` 우회 | `sec.write_data` 경유 + ok<70% 시 exit 1 + 기관 단위 prev-merge |
| 14 | US 빌더 | `build_congress_trades.py:311,331,351,663-698` | 3개 소스 전멸 시 0건이 6MB 파일 덮음(0-row 방어·리턴코드 없음) | 쓰기 전 0건 raise, `sec.write_data` |
| 15 | US 빌더 | `sec_client.py:81-83`(+insider :138, material :79) | EDGAR 페이지네이션 중간 실패 시 `break` → 잘린 결과를 완결로 기록, `lastFileDate` 전진 | partial 플래그, partial 이면 커서 미전진 |
| 16 | US 빌더 | 7곳 | `git_publish` False 무시: material_events:284, activist:153, ipo:347, us_dilution:167, us_financials_history:173, short_interest:147, earnings_calendar:195,197 → 미발행인데 exit 0(KR 계열은 raise) | KR 패턴으로 통일 |
| 17 | 워크플로우 | `daily-market-snapshot.yml:5`, `us-close-briefing.yml:5`, `daily-earnings-calendar.yml:5` | 20:05/20:30/20:34 UTC = **EST 겨울엔 15:05 ET, 장 마감 55분 전**. US 파이프라인에 ET 인식 코드 없음. 지금 무사한 건 GitHub 크론 지연(실측 21:57~22:34) 덕 | 21시대 UTC 로 이동 또는 스크립트 진입부 ET 16:05 게이트 |
| 18 | US 브리핑 | `us_close/main.py:111-112,269-281,329`(premarket 동형) | Gemini 전멸 시 "AI 분석 불가" 플레이스홀더를 발행하고 텔레그램 "작성완료" 통보 | `ai_text` 빈값이면 publish 건너뛰고 raise |
| 19 | 의회 | `institutional.js:406-421` | '의원별 추정 수익률 랭킹'에 표본 하한 없음 → 매수 1건 의원이 2위(103%). 40명 중 6명이 ≤2건 | 거래 N건 이상 필터 또는 thin 배지+정렬 제외 |
| 20 | automation | `automation/main.py:146-148`, `publish_to_notion.py:36-37`, `compliance_check.py:15-21` | 키움 발행 경로에 준법 검사 0(importer 는 미추적 scratch 뿐), 검사기는 fail-open, 프롬프트가 '서포터즈'·면책 표기 금지 | 발행 직전 정규식 하드룰 게이트(fail-closed), 면책 1줄 강제 |
| 21 | KR 빌더 | `build_kr_ownership_profile.py:196` 외 DART 빌더 | 주간 ~13,400회 무스로틀 호출(sleep 0건, 한도 20,000 의 67%). 020 제한 시 2,600→400행이 정상 발행 | 예산+페이싱, 020 즉시 중단, 직전 대비 70% 회귀 게이트 |

---

## 2. 중요 (P1)

### 2-1. 프론트엔드
- **폰 트리맵 목록 뷰** `treemap.js:322` — `metricValue` 사용으로 펀더멘털 18개 지표 전부 "—". `mapMetricValue`+`fmtMetric` 으로.
- **배당 플래너 항상 $0** `portfolio.js:177-187` — 스냅샷에 `dividendYield/Rate` 0건, `map_fundamentals` 키는 `divYield`. `US_STOCK_CALENDAR`(`us_calendar.js`)·`KR_DIVIDENDS` 로 교체. `yieldRatio>1` 판정이 1% 미만 수익률을 100배 부풀림.
- **X-RAY 성능** `portfolio.js:958-964`→`ai-mode.js:1301-1327` — 보유 종목수 × 전 종목 5회 정렬 매 렌더. 메모이즈.
- **KR 지수 카드 혼합 표기** `app.js:1904-1919` — price/series 는 ETF, changePct 는 지수(P0-2 와 함께 수정).
- **갭 존 오버레이 사문** `chart.js:102-118` — `for j=i` 라 모든 갭이 즉시 메움 처리(`analysis.js:169` 는 `i+1` 로 고쳐짐). 프리셋 스윙·데이가 켜지만 아무것도 안 보임.
- **캘리브레이션 점수 ≠ 화면 점수** `scripts/prob_calibration_worker.js:30-37` — `window.SHORT_INTEREST` 미주입 → 공매도 신호(weight 1.3)가 오프라인 점수에서만 빠짐. 주입하거나 신호 제외+caveats.
- **관리자 키 쿼리스트링 상시 전송** `community.js:711-716` — `POST /community/reports` 가 워커에 없어 400 → 폴백이 정식 경로. 게시판 렌더(12초 폴링)마다 반복(`:960`). GET+`X-Admin-Key` 로 통일.
- **약 700줄 사문 + 미정의 참조** `ai-mode.js:2010,2046,2062` — `scanProbColor`/`scanVerdict` 삭제(커밋 f25c88487) 후 `aiProbabilityHero` 는 항상 throw, `renderInlineStockWidget` 은 도달 불가. 살리거나 삭제.
- **AI 모드 종료 시 스트림 미중단** `ai-mode-welcome.js:416-433`, `ai-mode.js:2837-2843` — 과금 지속, 분리 DOM 에 rAF 쓰기.
- **모바일 11px 한글** `styles.css:13696-13697` — 지수 카드 12px 하한 위반.
- **터치에서 도달 불가 버튼** `styles.css:12332-12350, 12482-12503` — AI 답변 복사·세션 메뉴가 `:hover` 로만 노출.
- **AI 모드 + 라이트 테마 틴트 누락** `styles.css:10255-10276, 10424-10439` — `--tint-*`/`--ink` 미재정의로 우주 배경 위 흰 상자. 별칭 토큰 4개(94-100)는 스코프 재정의를 못 따라감.
- **`body{overflow-x:hidden}`** `styles.css:3297` — 넘침 은폐. 원인 후보 `.tabs .tab calc((100vw-32px)/3)`(3234).

### 2-2. 워커
- Origin 의존 `?ticker=` 응답에 `Vary: Origin` 없이 `public, max-age=900` (`:1594-1601`) → 빈 요약 15분 공유/요약 누출.
- `/sync/prefs` PUT 인증·Origin·TTL 없음, 크기 검사 UTF-16 단위(`:2258-2283`).

### 2-3. 미국 파이프라인
- IPO 캘린더 S-1/A 180건이 '등록 신청' 라벨(`build_ipo_calendar.py:284,288`).
- `build_map_fundamentals.py:190-212` 0-row 방어 없음(히트맵·valueScore 전멸 가능), `update_data.py:3304-3307` 이 실패 삼킴.
- prev-merge 만료 없음(`sec_client.py:296-319`) → 삭제 행 영구 부활(옵션 top60 밖 만기 지난 maxPain).
- `write_details` 선삭제 후 재작성(`update_data.py:3067-3069`, KR `:1969-1976` 동일) → 중간 크래시 시 반쯤 빈 details 커밋.
- 의원명 미정규화로 11개 성씨 중복(`build_congress_trades.py:537-596`).
- 13F 레지스트리 160개 중 29개가 타 회사 CIK 재사용(`institutions_13f_registry.py`).
- 백악관 일정 0건+신선 타임스탬프 푸시 가능, 게이트 그룹 없음(`fetch_white_house_schedule.py:104-123`).
- `verify_pages_deploy.py:78-81` head_sha 필터 없음(메모리의 '가짜 불일치' 원인).
- `earnings_history_store.py:174-181` 전량 실패도 신선 타임스탬프(게이트 의도적 무력화).
- `weekly-edge-stats.yml:74-80,101-108` 인라인 `git pull --rebase`(통합 publish 위반).
- `build_us_financials_history.py` 429 재시도 0회, 500 중 304 만 발행.
- `build_earnings_calendar.py:86-88` yfinance ImportError 삼킴 → 0건 초록.
- `build_leveraged_etf_catalog.py:392-414` `.json` 짝 없음.
- `build_factor_validation.mjs:475-477` 검증 전 파일 쓰기.
- `build_sec_ftd.py:63-66` 실패 시 더 오래된 파일로 회귀.

### 2-4. 국내 파이프라인·automation
- ETF 12종 시총 하드코딩이 실측을 덮음(`update_korea_data.py:556-570`, KODEX 200 5.0조 vs 실제 24.31조).
- 합성 3개월/YTD(당일×2.1, ×3.3, `:1165-1189`, 현재 0건 잠복).
- DART 날짜 파싱 미앵커(`build_kr_corp_disclosures.py:100-107`) — 배당기준일 '-' 면 지급예정일 집음.
- 공급계약 108→50건(54% 소실 무음), `write()` 0-row 방어 없음(`:65-82,198-205`).
- `krx_metrics`·`short_interest` 전량 실패 exit 0 + 5개 파일 freshness 미등록(`build_kr_krx_metrics.py:153`, `build_kr_short_interest.py:295`).
- `korea_close/main.py:287` 무조건 return → `--test` 가 실제 발행, `sys.exit(1)` 도달 불가.
- 브리핑 키 누락 시 플레이스홀더 발행 exit 0(`korea_close/main.py:166-169`, premarket:150-153).
- `korea-close-briefing.yml:18` timeout 120 vs 백업 240(실측 최대 1h35m).
- 쿨다운 원장이 `.gitignore outputs/` → 3일 쿨다운 무효(`automation/ticker_cooldown.py:27-28`).
- `NOTION_VERSION` 미사용, SDK 기본 2025-09-03 전송(`automation/notion_client.py:12,38`).
- `capture_chart.py:26,66` 레포 루트(.env 포함) 루프백 HTTP 서빙.
- 커뮤니티 수치 조작(`build_kiwoom_exports.py:376,404-405`: mention_count=score×100, change_rate=가격 24h).
- consensus prev-merge 만료 없음(상폐 종목 부활·동결 가격 괴리율), `write_data` 우회(`build_kr_consensus.py:732,739-741`).
- earnings_reactions·corp_disclosures 7일 창만·prev-merge 없음(라이브 8행).

### 2-5. 배포·문서·레포
- `stamp_build_id.py --check` 가 `sw.js` 미검증 + `ci.yml:45` continue-on-error(사유 해소됨).
- `deploy-pages.yml:1` UTF-8 BOM → `check_deploy_triggers.py:47` 이 'Deploy Pages' 못 봄(현재 무해).
- `deploy-pages.yml:87` no-op 게이트가 `actions: read` 없이 동작(공개 레포라서).
- **FEATURES.md 작업트리가 7월 판으로 되돌아감**(CRLF, mtime 09-12) → `git checkout -- FEATURES.md` 가 정답. SNS/Naver 67건 삭제는 `git rm --cached` + gitignore 로 확정 권장.
- origin 브랜치 157개 중 153개 정리 대상. `codex/continue-mir-us-stocks`·`feat/kr-disclosure-stats` 는 main 과 공통 조상 없음 → 재작성 전 이력 ~1GB 보유(pack 159MiB→1.09GiB 재증가 원인). 삭제 전 확인 필요.
- `index.html:592-595` AI 브리핑 일정 4줄 전부 실제 cron 과 다름(06:00/07:00/16:00/21:00 vs 05:34/06:06/15:42/21:07). 실제 발행은 GitHub 지연으로 국내 마감 브리핑이 22:00~23:40 KST.
- `prob_calibration.json` 09-03 이후 정지(스케줄러 없음, `analysis.js:2526` 이 fetch).

---

## 3. 개선 (P2) — 영역별 요약

- **app.js**: KST off-by-one `toISOString`(1791,1804) · 오프라인 배너 미이스케이프(7544-7552) · `sanitizeBriefingHtml` 은 태그 필터 없음(6608,6616) · 뉴스 href 스킴 검증 없음(5512,5070; ai-mode.js 929,2259,2593; disclosure-trackers 5곳; kr-panels 2곳) · sector `<option>` 미이스케이프(3218-3246) · IA 재편 잔재 ~200줄(카드뉴스 캐러셀 980-1107, 액션 news 분기, setViewMode) · `#stockInst13f` 빈 카드(`chart-indicators.js:818`) · ⌘K 팔레트 디바운스 없음(8283) · `renderSectors` 20×N 재스캔(3710) · 최상위 프라미스 catch 없음(7484,713,912) · WSB 표 클릭 미바인딩(6796).
- **피처 모듈**: 트래커 메타가 평균 표시(`disclosure-trackers.js:758`, 중앙값 원칙 위반) · 신고가 거리 정렬 방향 반대(`screener.js:206`) · 구성종목 상대수익률 폴백이 ETF 그룹 값(`markets-etf.js:824`) · 상관행렬 합성이력 미필터(665-675) · 시장 전환 후 백테스트 칩 미리셋(`portfolio.js:1775`) · 13F 발행사 첫 단어 매칭 오귀속(`institutional.js:79-86`) · X-RAY 밸류 축 척도 혼합(`ai-mode.js:1308`) · 폰 목록 전 종목 DOM 생성(`treemap.js:310`).
- **분석 엔진**: 종목 실측 모드 n=1 통과+모집단 edge 병기(`analysis.js:1486,2346-2371`) · 방향 섞인 패턴 5키 한 풀(1478; `build_pattern_stats.py:181`) · retest 정의 JS/PY 불일치(1519-1538 vs `build_breakout_retest.py:133`) · A/D·VWAP 합성봉 가드 누락(`indicators.js:438,491,508`) · '상승확률' 잔존(`chart.js:388`, `chart_capture.js:354`, `chart_capture.html:58`).
- **AI 모드·커뮤니티**: 새 답변 복사 버튼 소실(574,606) · `stripEmoji` 미적용 6곳 · 영어 에러코드 노출 · KR 에 8-K 빈 상자(820-832,937-948) · 자동완성 디바운스 없음(2912) · 세션 30×200 = 최대 12MB(20-41) · 키워드 '종합: 호재/경계' 배지(314-342, 정책 위반) · KR 티커 필 코드 표기(`community.js:1015,637,982`) · 차트 모드 8fps 전체 재그리기(`ai-cosmos.js:1812`) · jetColor 프레임당 1,936회 중복(1623-1645) · Ctrl+Enter 중복 전송 · 신고 되돌리기 불가.
- **워커**: `withLastGood` 부분 실패가 lastgood 오염(126-146) · stale 마커 읽는 클라이언트 없음 · 신고 자동숨김이 비원자 리밋 의존(74-85,1766) · IP 솔트 기본값 고정(190) · `looksDegenerateReply` 4경로 중 1곳만(2713,2846) · earnings_calendar 배치 상한 초과 가능.
- **CSS**: dead 클래스 54개/540줄(카드뉴스 캐러셀·커뮤니티 카드뉴스·SNS 채널·밀도 토글·액션 스위치·탐색 모드 바·regime-card·social-tooltip) · 미정의 `--vote-color`, `--gauge-dash` 폴백 없음 · 미사용 토큰 7개 · 641px 미승인 브레이크포인트 중복 블록(3245-3255), JS `MOBILE_TABS_MQ` 960 vs CSS 900 · append-only 충돌 11쌍(`.briefing-header` 반쪽 덮어쓰기) · 32px 미만 탭 타깃 16종 · select 고정 높이+16px 강제 5곳 · 다크 대비 미달 `#b45309`/`#0e7490` · backdrop-filter 45회, `pulseSparkle` drop-shadow 무한 애니메이션.
- **파이프라인 공통**: SEC 요청에 Chrome 사칭 헤더(`sec_client.py:32-39`) · quiverquant 유료 API 무인증+Referer 위조 · `check_data_freshness` `require_rows` 가 최상위 count 만(13f/congress/whitehouse 무의미) · cp949 reconfigure 누락 8파일(`update_data.py`, `update_korea_data.py` 포함) · `quote["close"]` 무방어(라이브 에러 MIDZ) · 10-K 내 분기값이 연매출로(`update_data.py:1667-1681`) · 재시도 백오프 선형/부재 12곳 · 사업연도 `year-1` 하드코딩 4곳(1~3월 전 종목 013) · naive `date.today()` 5곳 · KR `git_publish` 무시 7곳 · corpCode.xml 매회 재다운로드 · `http://` 서비스키 3곳 · 주말 브리핑 요일 가드 없음 · 백업 KR 워크플로우에 ECOS/나라장터/관세청·freshness 스텝 없음.
- **배포·문서**: `sw.js:17` 미사용 마스코트 220KB 프리캐시 · `analysis.html` manifest/theme-color/SW 등록 없음(600 URL 착지) · SW 오프라인 폴백 쿼리 미스 · `#backtestStartWrap/EndWrap` 항상 노출 · 라벨 없는 컨트롤 11개, 라이트박스 dialog 시맨틱 없음 · `index.html:2713` "하루 1회" 정적 문구 · `CHAT_SUGGESTIONS` "RS 점수" · sitemap 주간 갱신(lastmod 09-12, changefreq daily) · watchdog cron :17 실효 최대 5.8h, `pending` 미조회 · `ci.yml` main 미검증, `sync_critical_css --check` CI 미연결 · `dist/` gitignore 없음.

---

## 4. 사소 (P3) — 대표 항목

영문 킥커 2건(`app.js:6226,5310`) · 섹터 차트 범례 KR 코드 표기(3939-4210) · `changePct ?? 0` "0.0%" 실측처럼(1914) · 거래량 결측 "0.0배"(5088,5191) · `features?.krDart` truthiness(6478) · `window.alert`(6912) · CSV 파일명 UTC(7021,7042) · ZWJ 잔재(`signals.js:889`) · 안내문 글자 빠짐(`treemap.js:375`) · RS Score 죽은 코드(860) · RSI 합성이력 가드 우회 3곳 · `krOwnTable` 200행 더보기 없음 · 저장 스크리너 키 시장 미분리(`app.js:353`) · D+n DST 하루 오차(`watchlist.js:471`) · `ai-cosmos.js:1-2` IIFE 밖 전역 `storage` · SSE done 프레임(RAG 출처) 미소비 · `?cadmin=` 키 URL 미정리(`app.js:907`) · 워커 모르는 경로 400 을 900초 캐시 · `?earnings_probe=1` 무인증 진단 · ticker 길이 무제한 · 투표 `hold` 문서 vs 코드 · 하루 1표 UTC 경계 · 죽은 자산 `og-image.png`(196KB)·`favicon-16.png` · 죽은 DOM 3개 · `sector-detail.html` viewport/noindex 없음 · 수동 `?v=` 5곳 · 레포 description/homepage/topics 공백 · PR #156 CONFLICTING 11일 · `scripts/archive` 4개·`mir_design/*.png` 5.6MB·`outputs/audit-*` 7.3MB 추적.

**죽은 함수(app.js)**: `formatTimestamp` 3891, `renderInst13fChange` 5378, 카드뉴스 6종(987-1107), `renderActionNews` 1677, `scanRankOptions/scanEvidenceText/scanBasisLabel`(P0-5 해결 시 부활), `aiReportBusy` 7605.
**죽은/미연결 스크립트**: `audit_hangul_type.py`, `patch_kiwoom_notion_schema.py`, `setup_kiwoom_secrets.ps1`, `run_korea_daily_update.bat`, `automation/compliance_check.py`(부활 필요), `test_history_honesty_gate.py`(tests/ 로 편입), `build_prob_calibration.py`(스케줄 없음 → weekly-edge-stats 에 추가), `build_sr_stats.py`(.js 404 짝 정리).

---

## 5. 문서 현행화 필요

| 문서 | 항목 | 실제 |
|---|---|---|
| CLAUDE.md:145 | `.git` pack 2.57GiB | 1.09GiB(재작성 후 재증가 중) |
| CLAUDE.md:151 | 아티팩트 ≈330MB | 456MB |
| CLAUDE.md:156 | app.js 850KB / styles 340KB / index 122KB | 412KB / 382KB / 155KB |
| CLAUDE.md:132,137 | KR `shortInterest: false` | `market_config.js:241` true(07-21 실연동) |
| DEPLOY.md 표 | white-house 06:00 1회 | 06:00/16:00/21:00 3회 |
| DEPLOY.md:62 | KR 스냅샷 "dispatch 전용" | 평일 22:00 크론 존재(08-06~) |
| DEPLOY.md | watchdog 미기재, 13F 크론 2개 미명시, us_dilution/market_history/pattern_stats 동반 실행 미기재, weekly-edge-stats S/R 은 주석 처리됨 | |
| DEPLOY.md:95-97 | Gemini 기본 체인 2.0/1.5 | 둘 다 셧다운 |
| FEATURES.md(HEAD) | 기본/고급·밀도 토글, 기관/정치인/내부자 탭, 섹터/캘린더 최상위, 상승확률 스캐너, llama-3.1-8b 요약 | 전부 폐기/변경. 누락: 시장 폭·시그널·AI 브리핑·신뢰도 센터·TTS·커뮤니티 3서브탭·KR 5%룰/임원/지배구조 |
| `market_config.js:160` | KR snapshotCadence "매일 15:40 KST" | 실측 22:38 KST |
| `update_data.py:2915` | policy "Update once at 06:00 KST" | 크론 05:05 |
| worker/README | 요약 모델 llama, 투표 `hold`, `COMMUNITY_SERIALIZED` 미문서화 | |

---

## 6. 확인했지만 문제 없는 것 (커버리지)

- 프론트 25개 스크립트 미정의 전역 0, `byId` 오타 0, 중복 id 0, HTML 중첩 오류 0, 깨진 src/href 0, 전역 이름 충돌 0.
- 피처 플래그 `=== false` 판정 전수 준수(`features?.krDart` 1건 제외). `localStorage` 직접 접근 0(storage.js 내부 제외). 리스너 1회 바인딩 가드·타이머 회수 완비. 시장 전환 캐시 리셋 정상. `refreshFeatureViews` 지연 데이터 경합 처리.
- XSS: 스냅샷·DART·커뮤니티·LLM 출력의 innerHTML 싱크는 위에 적은 예외 외 전부 `escapeHtml`. `formatMarkdownToHtml` 자가검사 13건. 세션 복원 XSS 방어 유지.
- 분석 엔진: 룩어헤드 보정(confirmBreak lag, SR breakout, fake breakout) 정상, kNN 이웃 겹침 없음(12표본·윌슨·라플라스), RSI Wilder 단일 구현, SMA/EMA/BB/Stoch/Ichimoku 경계 정확, 요청 seq 취소, SVG 라 DPR 무관. JS↔Python 패턴 감지기·라벨 58키 전부 일치.
- FEATURE_DATA 43경로 전부 레포·라이브 200. 라이브 데이터 신선도 US·KR 전 파일 기대 주기 내(예외는 위 P0/P1 에 기재). `historyFallback.fabricated 0`, KR 커버리지 83.8%. 난수 조작 재발 없음.
- 워커: `CF-Connecting-IP` 만 사용(XFF 우회 없음), 관리자 키 timing-safe 비교, SSRF 없음, KV 키 인젝션 없음, CORS 반사 없음, clientId 유출 차단, Origin 게이트·v8 includePrePost 폴백 라이브 배포 확인.
- Actions: concurrency 워크플로우별 분리, permissions 최소, `[skip ci]` 없음, timeout 전수, fetch-depth 1(daily-korea-news 제외), 텔레그램 실패 통지 14개, Secrets 11:11 문서 일치, 배포 트리거 18개 이름 일치, 최근 12회 배포 success, 좀비 run 0.
- CSS: critical CSS 최신, z-index 토큰 100%, 음수 자간 규칙 준수, Pretendard 단일 스택, tabular-nums, keep-all, 이모지 0, 다크 토큰 34개 대응.
- 보안: `.env` 미추적, 추적 파일에 키 패턴 0, 워크플로우 시크릿 echo 0. (단 `.env` 가 OneDrive 동기 경로 평문 — git 밖 위험.)

---

## 7. 권장 착수 순서

1. **데이터 정직성 즉시 수정(하루)**: 워커 `fetchPrevDailyClose` 제거 + 수동 재배포, KR `indices` 를 m.stock 지수 API 로, `app.js` ETF price 폴백 제거, 분석 헤드라인·AI 프롬프트의 '상승확률' 제거, 스캐너 순위 마크업 3개 추가, 의회 랭킹 표본 하한.
2. **죽은 외부 의존 복구(하루)**: Gemini 폴백 모델 교체, 야후 crumb 실패 캐시, 국내 브리핑 스크레이퍼 m.stock 전환 + 프롬프트에 ecos/US 지수 주입 + 창작 금지.
3. **실패를 실패로(이틀)**: US 빌더 `git_publish`/0-row/페이지네이션 partial 처리 통일, 13F·congress·map_fundamentals 게이트, KR 유니버스 하한, krx/short exit 1 + freshness 등록, 브리핑 플레이스홀더 발행 금지, US 크론 21시대 이동.
4. **배포·SEO**: 아티팩트 사이즈 가드 + details 프루닝, KR canonical `market=kr`, sitemap 일간 갱신 + CI `--check`, `stamp --check` sw.js 포함 + continue-on-error 제거.
5. **정리**: `git checkout -- FEATURES.md`, SNS 삭제 확정, 브랜치 153개 정리(공통 조상 없는 2개는 확인 후) + `gc`, dead CSS 540줄·app.js 잔재 200줄·ai-mode 사문 700줄, 문서 수치 현행화.
6. **UX 품질**: 모바일 11px·터치 버튼·AI 라이트 테마 틴트·탭 타깃 44px, 배당 플래너 소스, 트리맵 목록 지표, 복사 버튼, 에러 문구 한국어화.
